"""Telegram bot: draft delivery + approve/edit/reject/save handlers + slash
commands (step 8). Note: this is wire.telegram (sub-package) — the upstream
python-telegram-bot package is imported as top-level `telegram`."""
