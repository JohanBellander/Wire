# Copy to .env and fill in real values. .env is gitignored.

ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# Required only when llm.provider=llamacpp in config.yaml. Bearer token for
# the OpenAI-compatible local backend (llama.cpp / vLLM / OpenRouter / ...).
# Leave blank for unauth'd local servers.
LLM_API_KEY=

# Optional: log verbosity (DEBUG | INFO | WARNING | ERROR). Defaults to INFO.
# WIRE_LOG_LEVEL=INFO

# Optional: path to config.yaml inside the container. Defaults to /data/config.yaml.
# WIRE_CONFIG_PATH=/data/config.yaml

# Optional: set to 1 to allow startup without /data/config.yaml present (local dev only).
# WIRE_DEV=1
